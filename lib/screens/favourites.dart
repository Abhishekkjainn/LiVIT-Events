import 'package:flutter/material.dart';

class FavouriteEvents extends StatelessWidget {
  const FavouriteEvents({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.yellowAccent,
    );
  }
}
